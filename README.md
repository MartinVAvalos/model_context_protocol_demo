Objectives:

Testing users:
  "Find test users with expired subscriptions but active feature flags."

Data corruption:
  "Check whether any orders have payment_status='paid' but no payment_id."

Duplicates:
  "Find users with the same email, phone number, or external customer ID."

Data analysis:
  "Show weekly signup conversion by acquisition source."

Migration validation:
  "Compare old_users and new_users after the migration and flag mismatches."